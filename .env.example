# Middleware MCP Server Configuration
# Copy this file to .env and fill in your actual values

# Required: Your Middleware API Key
# Get this from: https://app.middleware.io/settings/api-keys
MIDDLEWARE_API_KEY=your_api_key_here

# Required: Your Middleware project URL
# Format: https://your-project.middleware.io
MIDDLEWARE_BASE_URL=https://your-project.middleware.io

# Optional: Alternative authorization token (if not using API key)
# AUTHORIZATION=your_authorization_token_here

# Optional: Application mode (stdio, http, or sse)
# Default: stdio
APP_MODE=stdio

# Optional: Server host (for http/sse modes)
# Default: localhost
APP_HOST=localhost

# Optional: Server port (for http/sse modes)
# Default: 8080
APP_PORT=8080

# Optional: Comma-separated list of tools to exclude
# Example: EXCLUDED_TOOLS=delete_dashboard,delete_widget,create_alert
# EXCLUDED_TOOLS=
